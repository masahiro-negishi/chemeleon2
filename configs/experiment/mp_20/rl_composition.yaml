# @package _global_
defaults:
  - override /data: mp-20
  - override /rl_module: rl_module
  - override /callbacks: default
  - override /logger: wandb
  - override /scheduler: constant
  - override /paths: default
  - override /hydra: default

data:
  batch_size: 32
  dataset:
    target_condition: composition

rl_module:
  ldm_ckpt_path: ckpts/mp_20/ldm/ldm_composition_khvmhyd7.ckpt

  rl_configs:
    num_group_samples: 5
    group_reward_norm: true

  reward_fn:
    reward_type: csp

logger:
  wandb:
    name: rl_composition
