# @package _global_
defaults:
  - override /data: mp-120
  - override /vae_module: vae_module
  - override /callbacks: default
  - override /logger: wandb
  - override /scheduler: constant
  - override /trainer: default
  - override /paths: default
  - override /hydra: default

trainer:
  max_epochs: 5000
  min_epochs: 1000

vae_module:
  atom_type_predict: true

logger:
  wandb:
    name: "vae_dng"
    group: "${task_name}/${data.name}"
    tags: ["vae", "dng"]

ckpt_path: ckpts/alex_mp_20/vae/dng_j1jgz9t0_v1.ckpt
