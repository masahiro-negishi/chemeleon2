# @package _global_
defaults:
  - override /data: alex_mp_20
  - override /ldm_module: ldm_module
  - override /callbacks: default
  - override /logger: wandb
  - override /scheduler: constant
  - override /trainer: default
  - override /paths: default
  - override /hydra: default

ldm_module:
  vae_ckpt_path: ckpts/alex_mp_20/vae/csp_2bmsfrwy_v1.ckpt

data:
  dataset:
    target_condition: null

logger:
  wandb:
    name: "ldm_csp"
    group: "${task_name}/${data.name}"
    tags: ["ldm", "csp", "null_condition"]
